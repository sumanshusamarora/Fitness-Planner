# Used by the local dev server, drizzle-kit migrations, and the seed script.
DATABASE_URL=postgresql://fitness:fitness@localhost:5434/fitness

# Server-side OpenAI API key for the runtime AI fitness & nutrition coach.
# Leave blank to use the deterministic coach fallback. Never share or commit.
OPEN_API_KEY=
